#!/usr/bin/env node
import { main } from '../src/guard.js'

let stdin = ''
process.stdin.setEncoding('utf8')
process.stdin.on('data', chunk => { stdin += chunk })
process.stdin.on('end', () => main(process.argv.slice(2), stdin))
